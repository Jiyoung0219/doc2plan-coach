SYSTEM_AGENT = """너는 문서 기반 AI Agent다.
원칙:
- 입력으로 주어진 '추출 JSON'을 근거로만 답한다.
- 문서에 근거가 없으면 '근거 부족'이라고 말한다.
- 정답을 통째로 제공하기보다 체크리스트/계획/피드백 중심으로 코칭한다.
출력:
- 표 없이, 불릿/체크리스트/짧은 문단으로 모바일 가독성 좋게 작성한다.
"""

ASSIGNMENT_COACH = """다음은 과제 문서에서 추출한 JSON이다.
1) 제출 체크리스트(필수/권장 구분)
2) 최소 데모 시나리오(프로토타입 수준)
3) 작업 계획(단계 1~3, 각 단계 산출물 포함)
을 만들어줘.

[추출 JSON]
{assignment_json}
"""

PM_COACH = """다음은 프로젝트 가이드 문서에서 추출한 JSON이다.
팀 정보를 참고해서:
1) 역할 분담(역할명 + 담당 업무 + 산출물)
2) 마일스톤 기반 일정(주차/기간 단위)
3) 리스크 TOP5와 대응 전략
4) 주간 회의 운영 규칙
을 제안해줘.

[추출 JSON]
{project_json}

[팀 정보]
팀 인원: {team_size}
기간: {duration}
"""

REVIEWER = """아래는 문서 기반 기준(JSON)과 사용자의 초안이다.
기준에 따라:
- 충족한 점
- 부족한 점(근거와 함께)
- 다음 액션(가장 작은 단위 3개)
을 제시해줘.

[기준 JSON]
{criteria_json}

[사용자 초안]
{draft}
"""
